version: "3.9"

services:
  linux-nvidia-cuda-dev:
    profiles: ["linux-nvidia-cuda"]
    container_name:  linux-nvidia-cuda-dev
    ports:
      - "8892:8892"
    build:
        context: .
        dockerfile: Dockerfile.intel-nvidia-cuda
    user: root
    volumes:
      - type: bind
        source: ${SOURCE_PATH}
        target: /home/workspace/${PROJECT_NAME}
      - type: bind
        source: ${DRIVE_PATH}
        target: /home/drive
        read_only: true
    working_dir: /home/workspace
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
      PYTHONUNBUFFERED: "1"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped